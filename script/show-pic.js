var num = document.images.length;

document.getElementById("prev").addEventListener("click",runPic);
document.getElementById("next").addEventListener("click",runPic);

function runPic(){
    showPic(num);
}

function showPic(num){
    for (i=0; i<num; i++){

    } else {
        i=0;
    }
}